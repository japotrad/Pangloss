/**
	 * The <code>tmf</code> package has been developed based on ISO 16642 and has two main responsabilities :
	 * <ol>
 		* <li>Represents the terminological data of a Freeplane map as a Terminological Markup Framework datamodel (methods <code>populate</code>); The root of the termbase is the <code>tdc</code> object;</li>
 		* <li>Outputs this termbase into an XML file (methods <code>toGMT;</code>)</li>
	 * </ol> 
	 */
package tmf