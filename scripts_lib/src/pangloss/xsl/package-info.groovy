/**
	 * The <code>xsl</code> package deals with Extensible Stylesheet Language-related matters. It has several responsabilities :
	 * <ol>
 		* <li>Transform a stylesheet into a file filter in the 'Term export' dialog.</li>
 		* <li>If the stylesheet has some user-defined parameters, handle the default values and the translation of these parameters.</li>
	 * </ol> 
	 */
package pangloss.xsl;
